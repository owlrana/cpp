class Solution:
    def dominantIndex(nums: list[int]) -> int:
        
        ans = max(nums)

        for i in range(len(nums)):
            mul = 2 * nums[i]
            if ans < mul and ans != nums[i]
                
            else:
                flag = 2
            
            if max(nums) == nums[i]:
                a = i
        
        if flag is not 2:
            return a

            
        